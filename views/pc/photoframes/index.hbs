{{#extend "pcStyle"}}
    <link rel="stylesheet" href="{{static}}pc/css/photoframes.css">
    <style>
    </style>
{{/extend}}

<div class="section clearfix">
    {{#each photoFrames}}
        <div class="item">
            <a class="img-a" href="{{../static}}photoframes/detail?q={{_id}}" sta>
                {{#withFirst imgs}}
                    <img src="{{@root/static}}img/load.png" data-original="{{@root/static}}{{path}}{{name}}" alt=""/>
                {{/withFirst}}
            </a>
            <h2>{{title}}</h2>
            <p>设计排版{{pageNum}}P <em>￥{{price}}</em></p>
            <p>{{size}} <a href="{{static}}photoframes/detail?q={{_id}}" >购买</a></p>
        </div>
    {{/each}}
</div>


{{#extend "pcScripts"}}
    <script>

    </script>
{{/extend}}